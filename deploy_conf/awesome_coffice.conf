server {
       # the port your site will be served on
       listen      8000;
       # the domain name it will serve for
       server_name 104.236.180.35; # substitute your machine's IP address or FQDN
       charset     utf-8;

       # max upload size
       client_max_body_size 75M;   # adjust to taste

       # logs
       access_log /home/chen/awesome_coffice_logs/access.log;
       error_log /home/chen/awesome_coffice_logs/error.log;

       # Django media
       location /media {
               alias /home/chen/awesome_coffice/media; # your Django project's media files - amend as required
       }

       location /static {
           alias /home/chen/awesome_coffice/static; # your Django project's static files - amend as required
       }

       # Finally, send all non-media requests to the Django server.
       location / {
               uwsgi_pass  104.236.180.35:8001;
               include     /home/chen/awesome_coffice/deploy_conf/uwsgi_params; # the uwsgi_params file you installed
   }
}
